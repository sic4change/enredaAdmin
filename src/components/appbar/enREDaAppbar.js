// in src/MyAppBar.js
import * as React from 'react';

import { AppBar } from 'react-admin';

const enREDaAppBar = props => (
    <AppBar {...props} color="primary"> 
    </AppBar>
);

export default enREDaAppBar;
